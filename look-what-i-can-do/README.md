# Look what I can do

Mini Demo-Seite ohne Build-Step (nur HTML/CSS/JS) + Docker.

## Lokal

```bash
cd look-what-i-can-do
docker compose up --build
```

Öffnen: http://localhost:8081

## Server

Im Repo auf dem Server:

```bash
cd look-what-i-can-do
docker compose up -d --build
```
