# demo-site

Minimaler Docker-Deploy-Test für das Repo **Brechtwahl**.

## Lokal starten

```bash
cd demo-site
docker compose up --build
```

Dann öffnen: http://localhost:8080

## Server

Auf dem Server im Repo-Verzeichnis:

```bash
cd demo-site
docker compose up -d --build
```
